import 'package:test/databaseHelper.dart';
import 'package:test/quiz_build.dart';
import 'package:flutter/material.dart';

class QuestionEdit extends StatefulWidget {
  const QuestionEdit({super.key});

  @override
  State<QuestionEdit> createState() => _QuestionEdit();
}

class _QuestionEdit extends State<QuestionEdit> {
  DatabaseHelper databaseHelper = DatabaseHelper();
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
